# Additional requirements for custom heads and Optuna optimization
optuna>=3.0.0
plotly>=5.0.0  # For Optuna visualization
optuna-dashboard  # Optional: for web-based optimization monitoring

# Core ML dependencies (if not already installed)
torch>=1.9.0
transformers>=4.20.0
sklearn
pandas
numpy
wandb
